header.header
  nav.nav.nav--tours
    a.nav__el(href='/') Socios
  .header__logo
    img(src='/img/escudo.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Salir
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/user-10.jpg` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Iniciar Sesion
      a.nav__el.nav__el--cta(href='#') Asociate Gratis